import { Center } from '@chakra-ui/react';
import { Logo_instagram_icon } from '../../assets/icons';

export const Loading = () => {
 return (
  <Center h="100vh">
   <Logo_instagram_icon style={{ aspectRatio: 1 }} />
  </Center>
 );
};
